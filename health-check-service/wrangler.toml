name = "health-check-service"
main = "src/index.ts"
compatibility_date = "2026-05-18"

rules = [{ type = "Text", globs = ["**/*.html"], fallthrough = false }]

[triggers]
crons = ["* * * * *", "0 0 * * SUN"]
